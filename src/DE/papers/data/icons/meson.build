install_data(
  join_paths('scalable', 'apps', '@0@.svg'.format(app_id)),
  install_dir: join_paths(pps_datadir, 'icons', 'hicolor', 'scalable', 'apps'),
)
install_data(
  join_paths('symbolic', 'apps', '@0@-symbolic.svg'.format(pps_namespace)),
  install_dir: join_paths(pps_datadir, 'icons', 'hicolor', 'symbolic', 'apps'),
  rename: '@0@-symbolic.svg'.format(app_id),
)
